services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    networks:
      - holiday-vibes
    restart: unless-stopped

networks:
  holiday-vibes:
    external: true